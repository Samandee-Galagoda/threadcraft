# Deployed API origin, e.g. https://threadcraft-api.onrender.com
# Leave EMPTY for local development (the Vite dev proxy handles it).
#
# IMPORTANT: Vite inlines VITE_* variables at BUILD time. Changing this in the
# Vercel dashboard has no effect until you trigger a redeploy.
VITE_API_URL=
